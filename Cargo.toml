[package]
name = "my_app"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ndk-glue = "0.7"

[package.metadata.android]
build_targets = ["aarch64-linux-android"]
# Убираем все лишнее, пусть использует стандартные настройки сервера
