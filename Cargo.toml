[package]
name = "my_app"
version = "0.1.0"
edition = "2021"

[lib]
name = "my_app"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
ndk-glue = "0.7"

[package.metadata.android]
build_targets = ["aarch64-linux-android"]
target_sdk_version = 33
